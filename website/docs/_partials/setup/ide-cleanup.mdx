Once you have completed the cleanup of the EKS cluster, you can remove the IDE CloudFormation stack.

Navigate to the [CloudFormation console](https://console.aws.amazon.com/cloudformation/home) and select the `eks-workshop-ide` stack, then click the **Delete** button:

