# Intentionally deploy pod with wrong image
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx-wrong-image
  namespace: observability-opensearch
  labels:
    app.kubernetes.io/created-by: eks-workshop  
spec:
  containers:
  - name: nginx
    image: wrong-image
