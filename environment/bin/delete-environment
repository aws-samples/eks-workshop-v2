#!/bin/bash

set -Eeuo pipefail

reset-environment

absolute_path='/workspace/manifests'

if [ ! -z "$MANIFESTS_REF" ]; then
  absolute_path="github.com/aws-samples/eks-workshop-v2/environment$absolute_path?ref=${MANIFESTS_REF}"
fi

kubectl delete -k $absolute_path
