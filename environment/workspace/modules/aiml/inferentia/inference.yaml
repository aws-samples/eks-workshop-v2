---
apiVersion: v1
kind: Pod
metadata:
  name: inference
  namespace: aiml
spec:
  containers:
  - command:
    - sh
    - -c
    - sleep infinity
    image: ${AIML_DL_IMAGE}
    name: inference
    resources:
      limits:
        aws.amazon.com/neuron: 1
  serviceAccountName: inference
